export class Photo{
    constructor(public name: string, public occupation: string, public photo: string) {
        this.name = name;
        this.occupation = occupation;
        this.photo = photo;
    }
}